<%= form_with(model: elevator, local: true) do |form| %>
  <% if elevator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(elevator.errors.count, "error") %> prohibited this elevator from being saved:</h2>

      <ul>
      <% elevator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :serial_number %>
    <%= form.text_field :serial_number %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.select :model, [ "Standard", "Premium", "Excelium" ] %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.select :type, [ "Residential", "Commercial", "Corporate", "Hybrid" ] %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.select :status, [ "Active", "Inactive", "Intervention" ] %>
  </div>

  <div class="field">
    <%= form.label :commissioning_date %>
    <%= form.date_select :commissioning_date %>
  </div>

  <div class="field">
    <%= form.label :last_inspection_date %>
    <%= form.date_select :last_inspection_date %>
  </div>

  <div class="field">
    <%= form.label :inspection_certificate %>
    <%= form.text_field :inspection_certificate %>
  </div>

  <div class="field">
    <%= form.label :information %>
    <%= form.text_area :information %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
